{
    "model_config": {
        "d_model" : 768,
        "num_heads" : 12,
        "d_ff" : 2048,
        "vocab_size" : 10000,
        "context_length" : 256,
        "num_layers" : 10,
        "rope_theta" : 10000.0
    },
    "optimizer_config": {
        "lr_max": 1e-4,
        "lr_min": 1e-6,
        "warmup_iters": 1000,
        "cosine_cycle_iters": 9000,
        "weight_decay": 0
    },
    "train_config": {
        "batch_size": 64,
        "valid_batch_size": 128,
        "clip_grad_norm": 1.0,
        "max_steps": 10000,
        "max_seq_len": 256,
        "log_step": 10,
        "valid_step": 50,
        "save_step": 1000,
        "save_path": "cs336_basics/checkpoint"
    }
}